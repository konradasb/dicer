# Copyright 2026 Dicer Authors
# SPDX-License-Identifier: MIT

---
name: security

# New advisories appear without any change here, so the scan also runs weekly.
on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "23 5 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  govulncheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: go.mod

      # The packages that embed binaries do not compile without them.
      - name: Build embedded binaries
        run: make embedded hypervisor-binaries

      - name: Check for known vulnerabilities
        run: make vuln

  # What a pull request adds to go.mod or the workflows: known
  # vulnerabilities, and licences an MIT project cannot ship.
  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: moderate
          allow-licenses: >-
            MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, MPL-2.0
